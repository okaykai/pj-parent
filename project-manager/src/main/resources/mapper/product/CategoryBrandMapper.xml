<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.xcu.manager.mapper.CategoryBrandMapper">
    <!--  映射查询到的字段 -->
    <resultMap id="CategoryBrandMap" type="cn.edu.xcu.yky.model.entity.product.CategoryBrand" autoMapping="true">
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,
        brand_id,
        category_id,
        create_time,
        update_time,
        is_deleted
    </sql>

    <select id="findByPage" resultMap="CategoryBrandMap">
        select cb.*,
        c.name as categoryName,
        b.name as brandName,
        b.logo
        from category_brand cb
        inner join category c on c.id = cb.category_id
        inner join brand b on b.id = cb.brand_id
        <where>
            <if test="categoryBrandDto.brandId != null and categoryBrandDto.brandId != ''">
                and cb.brand_id = #{categoryBrandDto.brandId}
            </if>
            <if test="categoryBrandDto.categoryId != null and categoryBrandDto.categoryId != ''">
                and cb.category_id = #{categoryBrandDto.categoryId}
            </if>
            and cb.is_deleted = 0
        </where>
        order by id desc
    </select>

    <select id="findBrandByCategoryId" resultType="cn.edu.xcu.yky.model.entity.product.Brand">
        select *
        from category_brand cb
        inner join brand b on b.id =cb.brand_id
        where cb.category_id = #{categoryId} and cb.is_deleted = 0
        order by cb.id desc
    </select>
</mapper>