<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.xcu.manager.mapper.ProductMapper">
    <!--  映射查询到的字段 -->
    <resultMap id="ProductMap" type="cn.edu.xcu.yky.model.entity.product.Product" autoMapping="true">
    </resultMap>

    <sql id="columns">
        id,
        name,
        brand_id,
        category1_id,
        category2_id,
        category3_id,
        unit_name,
        slider_urls,
        spec_value,
        status,
        audit_status,
        audit_message,
        create_time,
        update_time,
        is_deleted
    </sql>


    <select id="findByPage" resultMap="ProductMap">
        select p.id,
        p.name,
        p.brand_id,
        p.category1_id,
        p.category2_id,
        p.pro_desc,
        p.category3_id,
        p.unit_name,
        p.slider_urls,
        p.spec_value,
        p.status,
        p.audit_status,
        p.audit_message,
        p.create_time,
        p.update_time,
        p.is_deleted,
        b.name brandName,
        c1.name category1Name,
        c2.name category2Name,
        c3.name category3Name
        from product p
        inner join brand b on
        b.id = p.brand_id
        inner join category c1 on
        c1.id = p.category1_id
        inner join category c2 on
        c2.id = p.category2_id
        inner join category c3 on
        c3.id = p.category3_id
        <where>
            <if test="productDto.brandId != '' and productDto.brandId != null">
                and p.brand_id = #{productDto.brandId}
            </if>
            <if test="productDto.category1Id != '' and productDto.category1Id != null">
                and p.category1_id = #{productDto.category1Id}
            </if>
            <if test="productDto.category2Id != '' and productDto.category2Id != null">
                and p.category2_id = #{productDto.category2Id}
            </if>
            <if test="productDto.category3Id != '' and productDto.category3Id != null">
                and p.category3_id = #{productDto.category3Id}
            </if>
            and p.is_deleted = 0
        </where>
        order by id desc
    </select>

    <!-- useGeneratedKeys="true" keyProperty="id" 回写ID到Product中  -->
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into product (
        id,
        name,
        pro_desc,
        brand_id,
        category1_id,
        category2_id,
        category3_id,
        unit_name,
        slider_urls,
        spec_value,
        status,
        audit_status,
        audit_message,
        create_time,
        update_time,
        is_deleted
        ) values (
        #{id},
        #{name},
        #{proDesc},
        #{brandId},
        #{category1Id},
        #{category2Id},
        #{category3Id},
        #{unitName},
        #{sliderUrls},
        #{specValue},
        #{status},
        #{auditStatus},
        #{auditMessage},
        now(),
        now() ,
        0
        )
    </insert>

    <select id="findProductById" resultMap="ProductMap">
        select p.id,
        p.name,
        p.brand_id,
        p.category1_id,
        p.category2_id,
        p.category3_id,
        p.unit_name,
        p.slider_urls,
        p.spec_value,
        p.status,
        p.audit_status,
        p.audit_message,
        p.create_time,
        p.update_time,
        p.is_deleted,
        b.name brandName,
        c1.name category1Name,
        c2.name category2Name,
        c3.name category3Name
        from product p
        inner join brand b on
        b.id = p.brand_id
        inner join category c1 on
        c1.id = p.category1_id
        inner join category c2 on
        c2.id = p.category2_id
        inner join category c3 on
        c3.id = p.category3_id
        where p.id = #{id}
        and p.is_deleted = 0
    </select>

    <update id="updateByProductId">
        update product set
        <if test="name != null and name != ''">
            name = #{name},
        </if>
        <if test="brandId != null and brandId != ''">
            brand_id = #{brandId},
        </if>
        <if test="category1Id != null and category1Id != ''">
            category1_id = #{category1Id},
        </if>
        <if test="category2Id != null and category2Id != ''">
            category2_id = #{category2Id},
        </if>
        <if test="category3Id != null and category3Id != ''">
            category3_id = #{category3Id},
        </if>
        <if test="unitName != null and unitName != ''">
            unit_name = #{unitName},
        </if>
        <if test="sliderUrls != null and sliderUrls != ''">
            slider_urls = #{sliderUrls},
        </if>
        <if test="specValue != null and specValue != ''">
            spec_value = #{specValue},
        </if>
        <if test="status != null and status != ''">
            status = #{status},
        </if>
        <if test="auditStatus != null and auditStatus != ''">
            audit_status = #{auditStatus},
        </if>
        <if test="auditMessage != null and auditMessage != ''">
            audit_message = #{auditMessage},
        </if>
        update_time = now()
        where
        id = #{id}
    </update>
</mapper>