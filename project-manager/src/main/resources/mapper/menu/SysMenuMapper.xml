<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.xcu.manager.mapper.SysMenuMapper">
    <resultMap id="sysMenuMap" type="cn.edu.xcu.yky.model.entity.system.SysMenu" autoMapping="true">
    </resultMap>

    <!-- 用于select查询公用抽取的列 -->
    <sql id="columns">
        id,
        parent_id,
        title,
        component,
        sort_value,
        status,
        create_time,
        update_time,
        is_deleted
    </sql>

    <select id="selectAll" resultMap="sysMenuMap">
        select
        <include refid="columns"/>
        from sys_menu
        where
        is_deleted = 0
        order by sort_value
    </select>

    <select id="selectMenuByUserId" resultMap="sysMenuMap">
        select distinct m.*
        from sys_menu m
        inner join sys_role_menu srm on m.id = srm.menu_id
        inner join sys_user_role sur on srm.role_id = sur.role_id
        where sur.user_id = #{userId}
        and m.is_deleted = 0
    </select>
</mapper>